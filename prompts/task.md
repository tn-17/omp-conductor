Implement explicitly requested deferred work with protected local Conduct workers. `conduct_task` runs one assignment; `conduct_batch` runs ordered independent assignments and waits for every worker and candidate capture.

<critical>
- Dispatch ONLY while Conduct is enabled and the user explicitly requests implementation; NEVER discover or auto-implement TODOs. Require a saved Git repository and saved files before dispatch.
- Supply exactly one of `selection` or verbatim `directive`, plus nonempty `files`, `assignment`, `context`, and `acceptance`, and the required `fixedDecisions` array (empty only when none are fixed). `files` must list exact writable files, cwd-relative or absolute inside the repository—never directories or globs. `assignment` is the bounded implementation task; `context` holds read-only evidence and labeled inferences; `fixedDecisions` holds only fixed user requirements and decisions; `acceptance` is a nonempty array of observable criteria. NEVER turn an inference into a user requirement. These fields and the exact chosen worker model are retained for review without rewriting their supplied whitespace.
- For a marker, obtain the opaque `selection` with `conduct_select` on the user-named file; selection/preview is not authorization. Reselect stale selections after rereading the updated intent; NEVER copy an old selection into `directive`. Marker text delimits instructions, not writable code, and its source is writable only when explicitly listed.
- Workers use the exact configured local worker model (loopback HTTP(S)); there is no model/provider fallback. They edit independent saved-file snapshots and return unapplied candidates. Conduct never applies automatically: inspect the actual patch and retained review/verification evidence, then ask the human to run the exact `/conductor apply id reviewToken` command. Only ready candidates may be applied; failed, cancelled, stale, or needs-attention candidates require intervention.
- Guarded worker access is limited to `conduct_read`, `conduct_grep`, `conduct_glob`, `conduct_edit`, `conduct_write`, native completion, and the explicitly configured optional advisor. Workers/advisors cannot widen scope or execute arbitrary commands. If more files are needed, stop and report the exact additional paths and reasons. Obtain fresh human authorization before changing scope; if authorization is declined, leave the existing candidate retained and do not reject it. Only after authorization, reject the old candidate through `/conductor reject id`, then dispatch afresh.
- The optional advisor is off by default and independently human-selected with `/conductor advisor provider/model-id` or `/conductor advisor off` as one exact available cloud/local model; no roster inheritance or fallback. Cloud selection discloses worker snapshot/task data. Pin the choice per candidate; advice cannot widen scope, count as verification/acceptance, or replace human review/apply. Advisor startup/runtime failure fails closed.
- Reviewer and verification settings are also human-selected, never dispatch-selected (`/conductor reviewer`, `/conductor fast`, `/conductor review-passes`, `/conductor verify`); only the orchestrator runs the human-configured verification. A reviewer is read-only and corrections retain exact scope; three total reviews (default) allow at most two corrections, and no unreviewed final fix is made. Every enabled reviewer or verification stage must be clean for readiness; malformed/startup failures and failed verification require attention, not a bypass. Worker, advisor, and reviewer fast preferences are independent, default off, and never enable a role or permit fallback.
- When trusted verification is configured, warn before dispatch: its command and project code run with host permissions and may read credentials, modify source/other host files, follow symlinks, access the network, or launch processes. The disposable copy is **NOT A SANDBOX**; retained arguments/output are shared with configured models.
- `conduct_batch` accepts 1 through the human-configured `/conductor workers 1..8` limit (default 1, hard cap 8); oversized batches reject. Settle shared interfaces and use disjoint exact ownership, including ancestor/descendant paths, before launch. Prepare all snapshots first; workers progress independently, preserve input order, and one failure does not cancel siblings. There is no queue, dependency scheduling, background work, or automatic application.
- `/conductor cancel` and `/conductor off cancel` stop unfinished stages and await candidate capture; already-settled outcomes stay settled. Review and apply each candidate separately.
</critical>
