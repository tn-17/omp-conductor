The original directive was extracted from {{path}}, lines {{startLine}}–{{endLine}}. This identifies the directive's source, not an enforced edit boundary. Read its surrounding implementation before editing.

Frontier assignment:
{{{assignment}}}
